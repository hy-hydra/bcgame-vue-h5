export const EMAIL_REG = new RegExp(
  /^([a-zA-Z0-9_-])+@([a-zA-Z0-9_-])+(\.[a-zA-Z0-9_-])+/
);

export const PHONE_REG = new RegExp(/^[0-9]+$/);

export const ONLY_LETTER_REG = new RegExp(/^[A-Za-z]+$/);
